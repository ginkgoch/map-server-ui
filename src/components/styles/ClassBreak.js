import React, { Component } from 'react';

export class ClassBreak extends Component {
    constructor(props) {
        super(props);

        this.state = { classBreak: this.props.classBreak }
    }
    render() { 
        return ( <></> );
    }
}